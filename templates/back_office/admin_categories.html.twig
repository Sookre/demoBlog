{% extends 'base_admin.html.twig' %}

{% block title %}BlogSymfony | Catégorie{% endblock %}

{% block body %}

    <table class='table table-striped table-dark table-bordered' id="table-category">
    <thead>

    <tr class="table-success">
    
        {% for data in titreCat %}

        {% if data !='id' %}

        {% endif %}

        {% endfor %}
    
    
    </tr>

    {% for data in titreCat %}
        <th class="text-center">{{data}}</th>
    {% endfor %}
        <th>Action</th>
    </thead>
    <tbody>
        {% for data in category %}
            <tr>
            <th class="text-center">{{ data.titre|capitalize }}</th>
            <td class="text-center">Description : {{data.description|striptags|slice(0,100)}}</td>
            <td class="text-center">Nombre d'article liées à la catégorie : {{data.articles|length}}</td>

                <td>
                <a href="" class="btn btn-warning"><i class=""></i></a>
                <a href="" class="btn btn-danger"><i class=""></i></a>
                </td>
            </tr>
        {% endfor %}
    
    </tbody>
    
    </table>

{% endblock %}